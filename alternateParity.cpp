#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    vector<int> makeParityAlternating(vector<int>& nums) {
        vector<int> ans = {0,0};
        for (int i=1;i<=nums.size();i++) {
            if (i != nums.size()-1 ) {
                
            }
        }
    }
};

int main() {
    Solution st;
    vector<int> nums = {-2,-3,1,4};
    for (auto i : st.makeParityAlternating(nums)) cout << i << " ";
    cout << endl;
    return 0;
}