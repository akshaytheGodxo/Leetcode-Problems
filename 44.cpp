#include <iostream>
#include <map>
#include <algorithm>
using namespace std;

class Solution { 
    public:
        bool isMatch(string s, string p) {
            
        }
    };


int main() {
    Solution s;
    string str1, str2;
    cin >> str1 >> str2;
    cout << s.isMatch(str1, str2) << endl;
    return 0;
}