#include <bits/stdc++.h>

using namespace std;

void solve(vector<int> arr, int n) {
    queue<int> dq;
    int tSum = 0;
    int cSum = 0;
    for (int i=0;i<n;i++) {
        
    }
}
// -1, 4, -1, 0, 5;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> arr(n);
        for (auto i : arr) cin >> i;
        solve(arr, n);
    }
}